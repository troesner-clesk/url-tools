services:
  url-tools:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./output:/app/output
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - OUTPUT_DIR=/app/output
    restart: unless-stopped
